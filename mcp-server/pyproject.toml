[project]
name = "mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.13.0",
    "motor>=3.7.1",
]

[tool.uv.workspace]
members = [
    "mcp-server",
]
